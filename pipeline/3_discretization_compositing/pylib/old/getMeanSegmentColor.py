
def getMeanSegmentColor(img_ids):

    mean_colors = []
    for id in img_ids:
        #apply mask
        print("bruh")
        #get mean of all axes
        #add that list/tuple to the list
    return(mean_colors)
