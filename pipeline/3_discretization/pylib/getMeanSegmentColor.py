
def getMeanSegmentColor(imgs_masks_ids):

    mean_colors = []
    for img, mask, id in imgs_masks_ids:
        #apply mask
        print("bruh")
        #get mean of all axes
        #add that list/tuple to the list
    return(mean_colors)
